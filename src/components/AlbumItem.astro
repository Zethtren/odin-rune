---
const { item } = Astro.props;
---

<div class="flex-auto rounded-lg bg-stone-400 bg-opacity-80 p-2">
  <h1 class="text-center text-xl">{item.name}</h1>
  <br />
  <p class="text-center">{item.description}</p>
  <p class="text-center">{item.date}</p>
</div>
